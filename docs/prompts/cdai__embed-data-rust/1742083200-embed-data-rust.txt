Spawn parallel agents to solve each of these issues. they should all make PRs targeting main: https://github.com/SeismicSystems/march-madness/issues/62 ---> i suggested include_str!, but you can use anything that gets the job done. i want functions like ::load() which will load the files that are in the repo at the time of building it. basically those two files (tournaent json and kenpom) should ship with the lib, essentially
