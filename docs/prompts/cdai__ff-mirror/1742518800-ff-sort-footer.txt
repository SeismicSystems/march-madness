this is so awesome. now on the very bottom of the page, i want to add a footer that allows sorting: (1) prob (2) expected pts (3) current pts. also, a way to revert to their local storage sorting... this is quite tricky... we should isolate this logic because itll be messy. bc what if they sorted manually, then click sort by prob, and then sort again? what happens? try your best to solve