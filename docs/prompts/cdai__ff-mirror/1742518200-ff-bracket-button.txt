this worked. now, i want to make 2 more changes: (1) remove "champion". it's implied. instead on the bottom, left side, we want to add a button "Bracket" that will take them to see the whole bracket for this player. this should **replace** the "click tile to see bracket" functionality, at least on mobile (desktop is fine, mobile only change here). basically its too easy to misclick -- a bracket button (or even an icon, just make it big enough, we have the space) is what we want. make it obvious that it's clickable