hey we just added a final 4 summary page. a few changes/features to add... i dont like the big vertical gap "vs.". nor do i like the headers "Senifinal 1" or "2".. those will be implied. all 4 items can be in a nice tight grid. then a natural little break (same as size you have between 2nd team in column and the current finalist row is good. then those two finalists should be essentially as they are, minus the arrows. the bordering / spacing should make it clear; indications / labels you add are noise. the champion section is perfect as is.... 2nd thing is we should allow users to sort the ordering as they see fit -- maybe we can take advantage of the top right to put small up/down buttons. we should persist this order to localstorage. key by slug not id, so works if api sends em back out of order. when yoyre done push a pr