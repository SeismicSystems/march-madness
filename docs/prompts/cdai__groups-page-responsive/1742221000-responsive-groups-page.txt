we want to reorganize this page so it looks better on desktop and mobile. different strategies:
1) mobile: use tabs. i think the default should be "Your Groups". if they're not in ANY groups, then mention they can view public groups, join a private group, or create a new group.
2) desktop... it's unclear what we should do here. BUT. the main problem... public groups and user's groups can grow to be a very large size, making the UI ineffective. the tabs solution solves this in mobile but we have more room to work on desktop. at worst, we can have a separate page for public groups, and then a main hub that has: create a group, join group (those 2 stacked vertically), and then a list of groups they're in... and then a separate link to "view public groups" which points to the public groups page.
