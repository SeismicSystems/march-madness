hm ok i re-thought this. the admin managed groups should be an entirely different contract. for many reasosn: (1) two totally differnet functioanlities (2) security (3) doesnt need to compose w the big contract... can you split it up so we have 3 contracts? 1) the main marchmadness one 2) sub-groups -- should be able to protect with password if we want (make that sbytes, user can enter a string and we convert it) -- but if they have no password thats ok it can be public 3) the manual admin group which stores its own brackets BUT doesnt need any winning computation [will all be off chain] or buyin etc -- we should call this a "mirror" and the other one "groups"
