and another issue, linking to the above issue: suppose we did have a live score feed. we should have a way to add this info to sims, when they run. that includes games that have already been settled. reference the JSON that the server receives in terms of tournament state. the simulation model should basically use that info (we need to include how much time is left, to adjust possessions sim). and then it should fold nicely into calibrator with kalshi odds