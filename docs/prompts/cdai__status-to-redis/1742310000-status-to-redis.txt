hey you see how how we write to this status file? we dont want to do that any more. instead, we should write the status to redis. use a key like mm:games -- analyze the usages across the repo (i think its the forecaster/sim as well as api server) and optimize the redis data structure for our consumer usecase. this corresponds to an issue -- look it up for more details (but its stale against the forecaster changes we just made) -- trust this prompt over that issue but its still good to reference. when youre done post a pr