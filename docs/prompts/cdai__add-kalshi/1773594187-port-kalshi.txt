# Task: Create PR 2 — `crates/kalshi` in the seismic march-madness repo

You are porting the Kalshi odds ingestor from `/home/c/code/sports/brackets` (source) to `/home/c/code/seismic-workspace/apps/march-madness` (target). Work in the target repo.

## Branch
Create branch `cdai__add-kalshi` from `main`.

## What to create

### New crate: `crates/kalshi/`

This is a lib crate with one binary (`kalshi`).

**Port ALL files from `/home/c/code/sports/brackets/crates/kalshi/src/`:**
- `lib.rs` — Verbatim
- `auth.rs` — The `workspace_root()` function finds the repo root for `.env` and RSA key files. It walks up from `CARGO_MANIFEST_DIR`. Since `crates/kalshi/` is 2 levels deep (same layout as source), this should work as-is. Verify.
- `rest.rs` — Has cache dir path logic. The cache goes to `data/{year}/cache/`. The source uses `workspace_root().join("data")...`. Same pattern works since workspace_root resolution is identical.
- `ws.rs` — Verbatim
- `fair_value.rs` — Verbatim
- `team_names.rs` — Loads `team_names.toml`. Source uses `CARGO_MANIFEST_DIR` to find it relative to the crate dir. Should work as-is since file layout is same.
- `types.rs` — Verbatim

**Binary: `src/bin/kalshi.rs`**
Port from `/home/c/code/sports/brackets/crates/kalshi/src/main.rs`. This is the full CLI with `fetch` and `watch` subcommands. Minimal changes needed — just move from `main.rs` to `src/bin/kalshi.rs`.

**Also copy:** `/home/c/code/sports/brackets/crates/kalshi/team_names.toml` → `crates/kalshi/team_names.toml`

**Key fact:** The Kalshi crate has ZERO imports from the bracket crate. It's fully standalone. The source `Cargo.toml` lists `bracket.workspace = true` but it's dead — not actually used. Do NOT add bracket-sim as a dependency.

**Cargo.toml:**
```toml
[package]
name = "kalshi"
version.workspace = true
edition.workspace = true

[[bin]]
name = "kalshi"
path = "src/bin/kalshi.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
csv = "1.2"
clap = { version = "4", features = ["derive"] }
reqwest = { version = "0.12", features = ["blocking", "json"] }
rsa = { version = "0.9", features = ["sha2"] }
sha2 = "0.10"
base64 = "0.22"
chrono = { version = "0.4", features = ["serde"] }
dotenvy = "0.15"
toml = "0.8"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time", "signal"] }
tokio-tungstenite = { version = "0.24", features = ["rustls-tls-webpki-roots"] }
futures-util = "0.3"
```

**Add to workspace `Cargo.toml`** — add `"crates/kalshi"` to the `[workspace] members` array.

### Data files

No data files need to be copied for this crate — it generates data files at runtime (`targets_kalshi.csv`, etc). The `data/2026/` directory may or may not exist in the target; create if needed for the cache dir.

## Edition 2024 concerns

The target workspace uses `edition = "2024"`. Check all source files for use of `gen` as an identifier — it's a keyword in edition 2024. The Kalshi crate likely doesn't use `rand` directly, so this probably isn't an issue, but verify.

## PR Requirements (from target repo CLAUDE.md)

1. Add entry to `docs/changeset.md` describing what was added
2. Save the prompt to `docs/prompts/cdai__add-kalshi/` as a `.txt` file (format: `{timestamp}-port-kalshi.txt`)

## Verification

Before committing:
1. `cargo build -p kalshi` compiles
2. `cargo test -p kalshi` passes (there may be no tests — that's OK)
3. `cargo clippy -p kalshi -- -D warnings` clean
4. `cargo +nightly fmt -p kalshi` applied

Create commits and push the branch. Then open a PR with `gh pr create`.
