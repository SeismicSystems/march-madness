on both the mobile and web versions of the app, we've got to include a link to the faucet (https://faucet.seismictest.net). and when they're connected we need to display their address and allow them to copy it (so they can paste it to the faucet). there should be a general link tot he faucet in side navigation-- but there should also be another thing where if we check their balance and it has 0 eth, we more prominently tell them to go to the faucet and get money. both web and mobile. ideally tbey can click faucet button and it opens new window for them.... another thing to fix in parallel... we should stick their picks in local storage so they save even if they refresh the page. key it by their address bc people will login with multiple methods and submit many brackets so localstorage needs to be keyed uniquely... also if they arent logged in, key anything they do with the zero address... and then when they do login, check if they have anything saved under the zero address, move it over to their address (if its nonempty only ofcoudse) and then delete from zeroaddress in local storage.... also... we want to allow them to pick the winners multiple rounds ahead without picking who their opponent will be . so i can just click duke duke duke duke all the way to the finals without filling in the rest of the bracket... that should be workable ui
