on the desktop version of the app, we used to show "X Brackets" to render the count of submissions. it got lost somehow. let's add this to the header of the page, right to the left of the faucet link. Just a small thing that counts the number of brackets. maybe make the number big (but not that it's bigger than the rest of the stuff on the header) and then in small font below say "# Brackets". when you're done make a pr