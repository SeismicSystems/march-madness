create an issue on the github repo, to update the ts wrapper client + web ui + deployment helpers (e.g. deploy-testnet.sh) all to support the fact that we have 3 contracts (MarchMadness, Groups, Mirrors) instead of just one. specifically the UI should show the "groups" feature somewhat prominently. and the "mirror" feature should be accessible but overall less prominent/more hidden. now, if a user has actually created mirrors, then we should show links to that on the hub once we're in a state past the deadline. pre deadline, we really just want to allow them to edit their bracket and join/remove groups. nothing about odds, probabilitty, just the bracket and the groups feature. we'll want to also have some way to show a registry of groups. i wouldn't worry about that now -- just put up a stub in the server returning empty list of public groups. if it's a private group (has a passowrd), then user needs to type the group id into the UI along with the password. use localstorage to keep track of group ids that the user is in -- we should always render info about those when they log in, on the "hub" page post-lock (and even pre-lock there should be a way to manage that)
